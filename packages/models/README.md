# Models Package

Shared data models and schema/domain types used across applications.

## Key Files
- `src/index.ts` package export surface
- `src/` model definitions

## Build
- `pnpm --filter models build`
