# UI Package

Shared UI components and presentation helpers for monorepo applications.

## Key Files
- `src/index.ts` package export surface
- `src/` reusable components

## Build
- `pnpm --filter ui build`
