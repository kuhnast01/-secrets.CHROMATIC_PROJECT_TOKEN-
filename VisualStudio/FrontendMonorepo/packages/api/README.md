# API Package

Shared API client utilities and API-facing domain helpers used across apps.

## Key Files
- `src/index.ts` package export surface
- `src/` API modules

## Build
- `pnpm --filter api build`
