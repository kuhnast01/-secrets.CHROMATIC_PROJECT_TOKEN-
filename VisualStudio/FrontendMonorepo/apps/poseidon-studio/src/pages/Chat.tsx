import React from 'react';
import { Box } from '@chakra-ui/react';

export const Chat: React.FC = () => (
  <Box p={8}>
    <h1>Chat Console</h1>
    <p>Message bubbles, code blocks, diff viewer, logs, tool execution, apply diff, open in editor.</p>
  </Box>
);

export default Chat;
