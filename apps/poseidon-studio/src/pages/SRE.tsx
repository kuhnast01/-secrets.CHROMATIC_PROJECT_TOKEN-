import React from 'react';
import { Box } from '@chakra-ui/react';

export const SRE: React.FC = () => (
  <Box p={8}>
    <h1>SRE Panel</h1>
    <p>Logs, error clusters, health checks, incident summaries, suggested fixes.</p>
  </Box>
);

export default SRE;
