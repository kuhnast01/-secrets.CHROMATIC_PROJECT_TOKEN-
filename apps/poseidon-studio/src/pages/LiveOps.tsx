import React from 'react';
import { Box } from '@chakra-ui/react';

export const LiveOps: React.FC = () => (
  <Box p={8}>
    <h1>LiveOps Tools</h1>
    <p>Event validator, shop preview, reward balancing, seasonal timeline, JSON schema validator.</p>
  </Box>
);

export default LiveOps;
