import React from 'react';
import { Box } from '@chakra-ui/react';

export const Repo: React.FC = () => (
  <Box p={8}>
    <h1>Repo Explorer</h1>
    <p>File tree, semantic search, file preview, related files, ask Poseidon about this file.</p>
  </Box>
);

export default Repo;
