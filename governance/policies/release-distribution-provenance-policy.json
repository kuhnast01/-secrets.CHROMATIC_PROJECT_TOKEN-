{
  "schemaVersion": 1,
  "policyId": "REL-IMMUTABLE-SIGNED-01",
  "description": "Production release publication must enforce immutable tag policy and signed provenance assets.",
  "requiredWorkflowSnippets": [
    "Enforce immutable release tag",
    "Validate release signing policy",
    "publishToGitHubRelease == 'true'",
    "Production release publish requires enableSigning=true",
    "No detached provenance signatures found for release publish"
  ],
  "requiredRunbookSnippets": [
    "release-distribution-provenance.yml",
    "publishToGitHubRelease=true",
    "enableSigning=true",
    "immutable"
  ]
}
